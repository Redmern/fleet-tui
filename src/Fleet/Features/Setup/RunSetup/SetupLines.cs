namespace Fleet.Features.Setup.RunSetup;

public static class SetupLines
{
    public const string Require = "local fleet = require 'fleet'";

    public const string Apply = "fleet.apply(config)";
}
