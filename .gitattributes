# Normalize line endings in the repository to LF, check out native.
* text=auto

# These must be LF even on Windows: the CI workflow runs them under bash.
*.sh    text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf

*.png binary
*.ico binary
